# Changelog

## Unreleased

- Initialize repository structure.
- Add `dat` CLI MVP (`list`, run by name, interactive mode).
- Add conventions for `scripts/install/<app>`.
- Add CI (`shellcheck`, `shfmt`, tests).
- Add one-line installer script and self update/uninstall scripts.
- Allow installer source from local path or full git URL.
- Add default installers: `fzf` and `work`.
