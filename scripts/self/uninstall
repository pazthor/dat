#!/usr/bin/env bash

set -euo pipefail

DAT_HOME="${DAT_HOME:-$HOME/.local/share/dat}"
DAT_BIN_DIR="${DAT_BIN_DIR:-$HOME/.local/bin}"

rm -f "$DAT_BIN_DIR/dat"
rm -rf "$DAT_HOME"

echo "dat uninstalled"
